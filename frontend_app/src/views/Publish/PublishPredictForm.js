import React, { useEffect, useState } from "react";
import { useTranslation } from "react-i18next";

import Grid from "@mui/material/Grid";

const PublishPredictForm = () => {
  const { t } = useTranslation();

  return (
    <Grid item xs={12}>
      contents
    </Grid>
  );
};

export default PublishPredictForm;
