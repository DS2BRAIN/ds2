import React, { useState } from "react";

import Button from "components/CustomButtons/Button";
import { Grid } from "@mui/material";

const Diagram = () => {
  return <Grid>Diagram</Grid>;
};

export default Diagram;
